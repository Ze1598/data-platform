[project]
name = "integration-tests"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["pytest>=8.0", "trino>=0.330", "psycopg[binary]>=3.2", "connectors"]

[tool.uv]
package = false

[tool.uv.sources]
connectors = { workspace = true }
