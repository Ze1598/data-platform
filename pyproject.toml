[project]
name = "data-platform"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = ["frontend", "dbt/data_platform", "query-engine/polaris_client", "orchestration/dagster_data_platform"]

[tool.uv]
package = false
