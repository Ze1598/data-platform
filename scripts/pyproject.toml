[project]
name = "scripts"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["psycopg[binary]>=3.2", "polars>=1.0", "numpy>=1.26", "domain-naming"]

[tool.uv]
package = false

[tool.uv.sources]
domain-naming = { workspace = true }
