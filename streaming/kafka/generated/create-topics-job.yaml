# Generated by scripts/generate_streaming_ingestion.py -- DO NOT EDIT BY HAND.
# One partition/replication-factor 1 per topic -- matches the single Kafka
# broker and each source's own Flink job parallelism default.
apiVersion: batch/v1
kind: Job
metadata:
  name: kafka-create-topics
  namespace: streaming
spec:
  backoffLimit: 2
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: create-topics
          image: apache/kafka:latest
          command:
            - sh
            - -c
            - |
              /opt/kafka/bin/kafka-topics.sh --create --if-not-exists \
                --bootstrap-server kafka.streaming.svc.cluster.local:9092 \
                --topic inventory-events --partitions 1 --replication-factor 1
              /opt/kafka/bin/kafka-topics.sh --create --if-not-exists \
                --bootstrap-server kafka.streaming.svc.cluster.local:9092 \
                --topic sales-events --partitions 1 --replication-factor 1
