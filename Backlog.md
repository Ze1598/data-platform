# Backlog

Things explicitly deferred, not forgotten. Unlike `Roadmap.md` (planned phases) or `Progress.md` (what's done), this is the catch-all for smaller items raised in passing — a want mentioned but not requested, a sharp edge found while building something else, a convention that only lives in a comment today. Nothing here is authorized for implementation; it's a list to point at, not a queue to work through unprompted.

---

### ~~`verify-pipeline`/smoketest rework must also exercise the sensor-triggered path~~ — RESOLVED 2026-07-17

Found 2026-07-16 during the master pipeline redesign (see Learnings.md, "A feed whose source is the pipeline's own run history..." — fourth occurrence): `financial_transactions_sensor` bypassed the master pipeline entirely (fired its per-feed job directly), which would have crashed every sensor-triggered run under the new design — caught by re-reading the code during this redesign, not by any test, since the sensor is `DefaultSensorStatus.STOPPED` by default and neither `just smoketest` nor `verify-pipeline`/`verify-schedule` ever turns it on. Fixed for the sensor bug itself same-day; the test-coverage gap closed the next day — `tests/test_sensors.py` (fast, evaluation-logic-only) plus `verify_sensor_trigger.py`/`orchestration/module.just::verify-sensor` (real end-to-end: starts the sensor, drops a file, waits for a live daemon tick, verifies success, stops the sensor again), now part of `just smoketest`'s own sequence. See `Progress.md`'s Phase 14 section and `Learnings.md` for the full build record, including two real bugs found in the test script itself (an empty-CSV crash, and the sensor's persisted cursor outliving a single test run).

### ~~Generalize storage-triggered ingestion as a per-feed choice, alongside schedules — rename `schedule` to `ingestion_triggers`~~ — RESOLVED 2026-07-17

Confirmed by direct instruction, implemented and fully verified same day. The `schedule` table is now `ingestion_triggers`: a `trigger_type` column (`'schedule'`/`'sensor'`) alongside the existing polymorphic `controlling_object_type`/`controlling_object_id`, `cron` now nullable (required only for `trigger_type='schedule'`, enforced by `chk_ingestion_triggers_cron`), and a new `chk_ingestion_triggers_sensor_feed_only` constraint (sensor-type is feed-only in the DB — a model has no landing directory to watch). `scripts/generate_dagster_pipeline.py` gained `fetch_feed_sensor_triggers()` and `_make_master_pipeline_sensor()` (a real generated-sensor factory, alongside the existing `_make_master_pipeline_schedule()`), emitting `ALL_SENSORS`/`SENSOR_ORCHESTRATION` symmetrically to `ALL_SCHEDULES`/`SCHEDULE_ORCHESTRATION` — reusing the exact proven cursor/glob/`RunRequest` shape of the original `financial_transactions_sensor`, plus one real improvement it never had (a live `is_trigger_active()` re-check, for true schedule/sensor symmetry). The original hand-written `financial_transactions_sensor` is retired from `financial_assets.py`; `financial_transactions`'s own trigger is now a seeded, generated sensor instead (`scripts/seed_metadata_db.py::seed_ingestion_trigger`). `PostgresMetadataResource.is_schedule_active()` → `is_trigger_active()`, one method for both trigger kinds. `test_sensors.py`/`verify_sensor_trigger.py` generalized to resolve a sensor via `ALL_SENSORS`/`SENSOR_ORCHESTRATION` rather than a hardcoded function/name (also closes the "Frontend CRUD for `schedule`" item below — see there for the new page). Confirmed via a full nuke-and-rebuild `just smoketest` (required — the DDL rename needed a fresh Postgres): schedule- and sensor-triggered runs both succeeded, the sensor now generated rather than hand-written. See `Progress.md`'s Phase 14 section for the complete build record, including one more stale `schedule` reference found and fixed in `test_schedules.py` during this verification.

### ~~Landing/archive lifecycle only applies to landing-sourced (file-drop) feeds~~ — CLOSED 2026-07-17, no longer needed

Raised 2026-07-17 during the documentation-staleness audit, then reconsidered the same day: on reflection, a dedicated `archive` layer/wipe-on-success lifecycle isn't needed. `raw` already IS the archive — every run writes its own durable, watermarked, verbatim copy (see the `storage_watermark` item below), so there's no separate "long-term copy" to maintain. The operational safeguard against data loss if a pipeline run fails is a discipline rule, not a mechanism to build: **landing data must not be modified or deleted by users while a pipeline could still need to reprocess it.** No implementation follow-up needed; `Roadmap.md`'s Layer Model section has been corrected accordingly (no `archive` layer, `landing` framed as file-drop-only with `raw` as its own durable record).

### ~~`data_processing_runs` needs a `storage_watermark` column to pin each run's raw read path~~ — RESOLVED 2026-07-17

Raised 2026-07-17 during the documentation audit, implemented and fully verified same day by direct instruction. Added `storage_watermark` (text, nullable) to `data_processing_runs` — a `YYYY/MM/DD/HH/MM/SS` path generated once in `PostgresMetadataResource.record_run_started()` at row-creation time (idempotent: `_ensure_run`'s ON CONFLICT branch never overwrites it, so a retried call reuses the same watermark). `raw_storage.py`'s `raw_snapshot_path`/`write_raw_snapshot`/`read_raw_snapshot` now key the raw parquet path by this watermark instead of `context.run_id` (`raw/<feed>/<storage_watermark>/<feed>.parquet`). Both the raw-writing and clean-reading steps get the value from their own `log_data_feed_stage(...)` call's `IngestionStepLog.storage_watermark` — `_find_run()` now returns it alongside `run_id` in the same `UPDATE ... RETURNING` round trip, no extra query needed. Updated: `metadata/db/init/01_platform_metadata.sql`, `postgres_metadata_resource.py` (`record_run_started`, `_find_run`, `_log_stage`, `IngestionStepLog`), `raw_storage.py`, the hand-written `customers`/`sales` assets, `scripts/generate_dagster_pipeline.py`'s two connector-feed templates (covers `financial_transactions`, `metadata_runs`, `police_crimes`), and `orchestration/module.just`'s `verify-pipeline` recipe (found live-broken: it independently reconstructed the old `run_id=`-based raw path itself rather than reading the new column — fixed). Confirmed via a full nuke-and-rebuild `just smoketest`: 5/5 feed-run + 3/3 domain model-run rows, durable raw files confirmed at the new path, schedule- and sensor-triggered runs both succeeded, 49/49 dbt tests, all pytest suites green. See `Progress.md`'s Phase 14 section for the full build record.

### ~~A `batch_group` must map to exactly one ODS domain (1:1), not enforced anywhere~~ — RESOLVED 2026-07-17

Confirmed by direct instruction 2026-07-16, implemented 2026-07-17. `frontend/pages/2_Data_Feeds.py`'s `build_values()` now checks, whenever `ods_enabled` is set, every other feed sharing this feed's `batch_group` for a conflicting `batch_ods_name` (mirroring the existing `deletes_enabled`/`extraction_type` cross-check pattern) and blocks the save with a clear error if found. One real bug caught during implementation: `existing_batches` (the batch-picker's own lookup) is a separate raw `pd.read_sql` call that doesn't go through `fetch_table`'s UUID-stringification, so `form_values["batch_group"]` can be a raw `uuid.UUID` while `df["batch_group"]` is already a string — comparing them directly would have silently never matched. Fixed by resolving `batch_group` to a string once, reused for both the conflict check and the final insert/update value (avoids a second, different `uuid.uuid4()` for a brand-new batch too).

---

### Superseded design: single shared dbt project + `tag:<model_schema>` selectors, as a fallback if full domain isolation proves premature

Before landing on genuine per-domain dbt project isolation (separate `dbt_project.yml`/manifest/image per `model_schema` domain — see `Roadmap.md`), a lighter-weight alternative was fully designed and explicitly rejected in favor of real isolation, not because it doesn't work: **one shared dbt project** (today's structure, unchanged), with each domain's `staging`/`model`/`serve` models tagged `tag:<model_schema>` — the exact same mechanism already splitting transformation from serving today (`_build_transformation_assets_for_feed`/`_build_serving_assets_for_feed`) — giving independently-triggerable `dbt build` invocations per domain, physical naming-convention differentiation (`<model_schema>_<fct|dim>_<name>`) instead of separate schemas, and Dagster `pool=` (already used per feed) to avoid concurrency races between domains. This gets domain-scoped triggering and naming-collision avoidance without any new manifests/images — real, viable, much less infrastructure than full isolation.

**Rejected because**: it doesn't solve `dbt parse`'s full-project compile cost (paid by every domain's build regardless of `--select` scope) or deployment blast radius (one shared manifest/image means every domain's build/deploy is coupled, even if not literally blocked by another domain's broken model). Given this platform is explicitly meant to validate feasibility at real enterprise scale, those were judged worth solving now rather than deferring.

Noted here in case genuine per-domain project isolation turns out to be more than is needed and this lighter mechanism becomes the better fit after all — it's a complete, ready-to-build fallback, not abandoned reasoning.

### ~~Frontend CRUD for `schedule`~~ — RESOLVED 2026-07-17

New `frontend/pages/4_Ingestion_Triggers.py`, following the `1_Source_Systems.py`/`2_Data_Feeds.py`/`3_Lakehouse_Models.py` per-table CRUD pattern (`render_form`/`build_values`, no `st.form()`). Covers both trigger kinds now that the table does (see the resolved "Generalize storage-triggered ingestion..." item above) — a radio for `controlling_object_type`/`trigger_type`, a live-reactive `cron` field (schedule-type only), and a non-blocking eligibility warning plus a hard `build_values()` check for sensor-type (feed-only, `connector_kind` must be `csv`/`json_file`). `load_type` still doesn't need one (fixed 4-row lookup, seeded once via DDL, never edited).

### `batch_group`/`batch_feed_hierarchy` are metadata-only

Every `data_feed` has a batch (enforced not-null), but nothing in Dagster groups or orders execution by batch/hierarchy yet — it's pure data with no behavioral consumer. Whether this becomes real (e.g. a batch-scoped job/schedule, parallel-within-tier execution) depends on whether a real multi-feed batch ever gets configured; today every feed is still its own singleton batch.

### ~~No automated check that a dbt model's `tags=[...]` actually matches its `owning_feed_id`~~ — RESOLVED 2026-07-17

New `scripts/check_model_feed_tags.py`, wired into `dbt/module.just::test` (runs unconditionally, after the per-domain `dbt test` loop). Resolves the "Docker step vs. dbt test" question this item originally left open with a third option: every domain's `profiles.yml` targets Trino/Iceberg only, so a dbt-side test structurally can't cross-reference `owning_feed_id` (no connection to `platform_metadata` Postgres at all), and `orchestration/Dockerfile`'s `dbt parse` step has no DB connectivity either — a live-Postgres `just`-run script, after each domain's manifest already exists, is the only place that actually works. Reads each active `lakehouse_models` row's compiled model/snapshot node from its domain's `target/manifest.json` (matching on `resource_type in ('model','snapshot')` + `schema='model'` + `name=table_name` — confirmed against a real manifest that a Type 2/SCD dimension compiles to a `snapshot` node, not a `model` node, so both resource types have to be checked) and asserts its `tags` is exactly `[owning_feed_friendly_name]`.

### `customers`/`sales` are still synthetic in-memory stub generators

Their `raw_*` assets now write real durable files (fixed this session), but the data itself is still generated fresh in-process each run, not pulled from any real source the way `financial_transactions` (CSV file-drop) and `police_crimes` (live API) are. Turning either into a real source isn't planned — noted only because it's the one remaining asymmetry between the four feeds.

### ADBC driver decision — paused, not resolved

Flagged during earlier research for the not-yet-built front-end data-viz module (Roadmap Phase 12): no `adbc-driver-trino` on PyPI, would need a separate Go-based `dbc install trino` binary installer, inconsistent with this project's all-`uv` tooling. Explicitly set aside by direct instruction — do not pick this back up without being asked; if Phase 12 resumes, the plain `trino` Python client is the fallback already identified.

### ~~JSON-list metadata fields should be comma-separated text in the frontend, not raw JSON~~ — RESOLVED 2026-07-17

Reported friction from actually working through `Walkthrough_Metadata_Source_Feed.md` end-to-end (2026-07-14). Real scope turned out narrower than originally named: `depends_on_feeds` was already a `st.multiselect` (not raw JSON), and `schema_registry` has no frontend page at all — the actual fix was `lakehouse_models.business_key_columns`/`tracked_columns` and `data_feed.source_pk`, now plain comma-separated `st.text_input` fields, parsed via a new `metadata_db.parse_csv_text()`/rendered via `to_csv_text()` (symmetric to the existing `to_json_text()`). Storage format (a jsonb array) is unchanged — only the editing widget is. `extraction_config`/`connection_config` (dicts, not lists) correctly stay JSON.

### ~~Per-model transformation gating~~ — resolved 2026-07-15 via the ODS layer, not a per-model `--exclude` list

Originally flagged: `lakehouse_models.pipeline_steps` (Phase 13's cherry-picking work) only gated a model's **serving** step, leaving transformation all-or-nothing per feed — fully closing that would have meant a dynamic per-model `--exclude` list inside each feed's transformation `dbt build`, real but materially bigger/more fragile than the concrete cases in scope needed. Resolved differently: a feed that never gets a hand-modeled dimension/fact (i.e., has no reason to need per-model gating in the first place) can now get an automatic **ODS (Operational Data Store)** table instead — `data_feed.ods_enabled`, `clean` data pushed as-is through a fully auto-generated `staging` + Type 1 `model` layer, driven by `schema_registry`. See `Progress.md` Phase 13 Part 4 for the full build/verification record. The narrower "skip transformation for one model among several sharing a feed" case this item originally described is still not built — but the underlying need (a feed producing zero real value in `model` when nobody wants to hand-model it) is what's actually closed.

### Roadmap phases not started

Not backlog items in the "found in passing" sense, but listed here for one-stop visibility — see `Roadmap.md` for the actual design content.

**Current priority (2026-07-17)**: Phase 14 (the master pipeline redesign, in-cluster Dagster deployment, metadata-driven per-domain Docker images, the in-cluster Streamlit Deployment, and sensor-triggered-path test coverage) is fully built and live-verified, `DataModel.md` is fully corrected, and a follow-up batch of five backlog items (comma-separated CRUD fields, the `ingestion_triggers` schedule/sensor generalization + its frontend page, `batch_group`→ODS-domain 1:1 enforcement, and the dbt model-tag/`owning_feed_id` check) is also done — see `Progress.md`'s Phase 14 section for the full record. What's left in this file below is genuinely unscheduled — no "next" item designated; work through it as prioritized when picked up. 11 and 12 remain deliberately deprioritized.

- **RESOLVED 2026-07-16: `schema_registry` ownership violation.** Found while live-verifying Phase 15 (an ODS build failed with a live Trino `COLUMN_NOT_FOUND` error tracing back to this). Fixed (full detail in `Roadmap.md`'s Phase 13/15 entries and `Progress.md`'s "schema_registry ownership fix" writeup — the implementation plan itself has been cleaned up now that it's done): every connector-driven feed's extraction step (renamed `landing_<feed>` → `extraction_<feed>` throughout — "landing" was never a real pipeline-stage concept, just a misleading name carried over from before the connector library) now owns schema discovery + `sync_schema_registry()` uniformly; `clean_<feed>` is read-only against `schema_registry` for every connector kind. REST/JSON kinds' extraction step also performs the `clean`-layer write itself, to avoid flattening the source data twice — `clean_<feed>` is a pure pass-through for those kinds now. `customers`/`sales` (hand-written, predate the connector library) had the identical violation, same fix applied. `seed_schema_registry()` and its 4 call sites deleted outright. **Second bug found during this fix's own live verification** (not a regression from it, and not scoped by the original plan): `scripts/generate_deletion_synthesis_views.py` had its own unwarranted `schema_registry` dependency, hard-crashing `just start` on a from-scratch platform (schema_registry is correctly empty until a feed's first real run) — fixed by removing the dependency entirely (`select *` + `lakehouse_models.business_key_columns`, since the downstream hand-written model already names its own exact columns), not just tolerating the missing row; also corrected a doc/code drift in the same script (output path now matches Roadmap.md's documented `models/model/intermediate/generated/`, not the `dimensions/generated/` it had drifted to). Full `just smoketest` green afterward: 5/5 feed-run + 3/3 domain model-run, schedule-triggered run succeeded, 49/49 dbt tests, all pytest suites. See `Roadmap.md`'s Phase 13/15 entries and `Learnings.md` for full detail.
- **RESOLVED 2026-07-16: Phase 14 — master pipeline orchestration, fully built and live-verified.** `EXTRACTION_JOBS`/`MODELING_JOBS`/`SERVING_JOBS` + one `master_pipeline` job replace `FEED_JOBS`/`DOMAIN_JOBS`/`pipeline_init_<feed>`/`k8s_job_executor` entirely (`k8s_job_executor` dropped, not fixed — see `Learnings.md`). Real in-cluster `Deployment`s now exist for Dagster's webserver/daemon/code-server; `verify-pipeline`/`verify-schedule` launch through `master_pipeline` for real and both pass, alongside the full pytest suite. See `Progress.md`'s Phase 14 section for the complete build record and the seven real bugs found/fixed along the way.
- **Phase 15 — Multi-project dbt split (per-domain dbt compile isolation)**: mostly built and live-verified 2026-07-16 — every domain gets its own dbt project (`dbt/domains/<domain>/`), genuinely compile-isolated (a broken model in one domain doesn't fail another's `dbt parse`, confirmed via a deliberate-break test); ODS domains derive from `data_feed.batch_ods_name`. Full pipeline verified live through the real Dagster/K8s path. Two real bugs found and fixed along the way (see `Learnings.md`): dbt local-path package macro-sharing doesn't reliably work past a project's first build (fixed by physically copying macros per domain instead of installing them as a package); domain names and feed names shared one flat Dagster `group_name` namespace, silently cross-contaminating job selections on a same-name collision (fixed via a `domain_<name>` prefix). **`verify-schedule` + full test suite now done.** **Superseded 2026-07-16**: the "ephemeral `k8s_job_executor` pod verification" item this bullet used to list as open is moot — `k8s_job_executor` is dropped entirely by the Phase 14 redesign (each stage is already its own top-level job/pod); trigger-by-model/trigger-by-batch-group resolver verification against live data is done as part of Phase 14's `verify-pipeline` rework. See `.claude/plans/multi-project-dbt-split-RESUME.md` for the original detail.
- **Phase 13 — Master pipeline architecture (extraction/validation/transformation/serving as child pipelines)**: fully built 2026-07-14/15 — connector library (extraction/validation split across Postgres/CSV/REST/JSON sources), the master pipeline as a real Dagster construct (`pipeline_init_<feed>`), cherry-picking (`pipeline_steps` lookup table + live per-run feed gating + codegen-time model-serving gating), dbt-side boilerplate reduction (`scripts/generate_model_scaffolds.py`), and the ODS layer (`scripts/generate_ods_models.py`, resolving per-model transformation gating a different way) are all implemented and `just smoketest`-verified. No open items remain from Phase 13 itself — the `schema_registry` ownership bug above was latent in this phase's own connector-library build, only surfaced by Phase 15's live verification.
- ~~**Phase 14 — Reconsider Dagster as the orchestrator**: next up, after Phase 15 closes.~~ **No longer applicable in this form** — superseded by the "RESOLVED 2026-07-16: Phase 14" bullet above (the master pipeline rebuild), which is what this phase's own deliverable turned out to be, not a decision to replace Dagster (Roadmap.md's Phase 14 section, "not a decision to remove Dagster, a flagged concern to revisit deliberately"). The two original gaps this bullet named — pipeline authoring is 100% code (no visual authoring surface) and `data_processing_runs` duplicating Dagster's own observability layer — are **still genuinely open and unaddressed**, not resolved by the rebuild; they're carried forward in `Roadmap.md`'s Phase 14 section rather than re-tracked here, to avoid the same fact living in two places.
- **Phase 11 — Streaming ingestion** (deprioritized): barely designed, rough shape only (Kafka/Flink or Kafka Connect Iceberg sink).
- **Phase 12 — Front-end data visualization module** (deprioritized): two open decisions (ADBC vs. plain `trino` client — see above; ephemeral vs. saveable charts).
