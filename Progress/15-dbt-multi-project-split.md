# Phase 15 — Multi-project dbt split (per-domain compile isolation)

**Status: done.** Originally recorded (2026-07-16) as "substantially built and live-verified, but NOT complete," with two items left open below. Reconciled (2026-07-20): both were superseded, not completed as originally scoped — see "Still open" note below for what actually happened to each.

**Full code migration**, all compile-checked and then live-verified against a real kind cluster, not just statically: every `generate_*.py` codegen script (`generate_sources.py`, `generate_model_scaffolds.py`, `generate_serve_views.py`, `generate_ods_models.py`, `generate_deletion_synthesis_views.py`) relocated to per-domain output under `dbt/domains/<domain>/`; new `scripts/generate_domain_projects.py` scaffolds each domain's dbt project skeleton and syncs shared macros into it; `dbt_assets.py` rewritten with domain-scoped job factories (`_build_transformation_assets_for_domain`/`_build_serving_assets_for_domain`) plus live per-run per-feed cherry-picking inside a multi-feed domain job; `generate_dagster_pipeline.py` generates `DOMAIN_JOBS` (via `dagster_k8s`'s `k8s_job_executor`) alongside narrowed `FEED_JOBS`, plus two new trigger resolvers (by lakehouse model, by batch group); `definitions.py` simplified accordingly; `orchestration/Dockerfile` loops `dbt parse` across every domain; `dbt/data_platform/` (the old single shared project) fully retired — every hand-written model/staging/snapshot file `git mv`'d into its new domain, renamed to its `table_name`, every cross-file `ref()` fixed, `.gitignore`/root `pyproject.toml` cleaned up to match.

**Compile-time isolation — the actual point of this phase — proven directly, not assumed**: a deliberately broken `ref()` in one domain's `stg_customers.sql` failed only that domain's `dbt parse` (exit 2, the expected unresolvable-ref error); a sibling domain (`metadata`) parsed clean (exit 0) in the same test, run back to back. Reverted immediately after.

**Full pipeline verified live 3 times consecutively** through the real Dagster/K8s path (`just orchestration::verify-pipeline`, not just `dbt parse`) — including rebuilding already-existing tables a second and third time, which is what two real bugs (next paragraph) were hiding behind; each of the three real feeds' domains (`sales`, `metadata`, `police_crimes`) succeeded across staging/model/serve every time. **ODS domain grouping via `data_feed.batch_ods_name` confirmed live**: flipping `police_crimes` to `ods_enabled=true` (it has no `lakehouse_models` row — a feed with no hand-modeled dimension/fact) correctly formed its own `police_crimes` domain project and built successfully, end to end.

**Two real bugs found and fixed during this live verification, full write-ups in `Learnings.md`**:
1. Sharing `dbt/_shared/`'s macros across domains as an installed local-path package dependency looked correct through `dbt deps`/`dbt parse`/even a first `dbt build`, then broke non-deterministically (`'<macro>' is undefined`) on any *second* build of an already-existing table — confirmed via the installed dbt-core source that one macro family (`generate_schema_name`) is structurally excluded from installed-package resolution for a project's own nodes, though the exact mechanism for the other affected macros (`row_hash`) wasn't further isolated. Fixed by physically copying every shared macro into each domain's own `macros/` directory instead of installing them as a package — `dbt/_shared/` is now a template source a human edits once, not a real dbt dependency.
2. Domain names and feed names shared one flat Dagster `group_name` string space — a same-name collision (the `sales` domain vs. the `sales` feed; the `police_crimes` ODS domain vs. the `police_crimes` feed it defaults its name from) silently cross-contaminated `AssetSelection.groups()` selections in both directions (a domain job picking up its own member feed's extraction chain; that feed's own job picking up the domain's dbt assets, plus other feeds via `.upstream()`). Fixed via a `domain_<name>` prefix (`dbt_assets.py::domain_group_name()`), consistently applied everywhere a domain's group is selected.
3. `verify-pipeline`'s hardcoded expected row counts were stale for the new topology (model-run rows now come from `DOMAIN_JOBS`, one per domain, not one per feed) — re-derived from 5 feed-run rows (unchanged) to 3 domain-run rows (`sales`/`metadata`/`police_crimes`), confirmed against three consecutive live runs before updating the assertion in `orchestration/module.just`.

**Originally left open here, reconciled 2026-07-20 — both superseded by Phase 14's rebuild, not independently completed**: (1) ephemeral `k8s_job_executor` pod verification — moot, not just unverified: Phase 14's master-pipeline rebuild (same day) replaced the `k8s_job_executor`/`DOMAIN_JOBS` design entirely with `MODELING_JOBS[domain]`/`SERVING_JOBS[domain]` as top-level `K8sRunLauncher` runs, each its own pod directly, never a pod spawning a child pod (README.md "Master Pipeline Architecture") — confirmed via full-repo grep that `k8s_job_executor`/`DOMAIN_JOBS` no longer appear anywhere in the codebase. There is no step-level pod construct left for this item to verify. (2) trigger-by-model/trigger-by-batch-group resolver verification against live data — genuinely covered, under the new design: `PostgresMetadataResource.resolve_model_domain_and_feeds()`/`get_batch_group_ods_domain()`/`get_batch_group_feeds()` back `master_pipeline`'s `orchestration_kind` mechanism, and `orchestration::verify-pipeline` (part of `just smoketest`) calls `trigger_master_pipeline.py` with `orchestration_kind=model_schema` (twice — `sales`, `metadata`) and `orchestration_kind=batch_group` (`police_crimes`), checking real `data_processing_runs` rows afterward for each.

**Blocked, as of 2026-07-16, from closing the above — RESOLVED the same day**: a real, separate `schema_registry` ownership bug was found during this phase's own live verification (an ODS build failed with a live `COLUMN_NOT_FOUND` Trino error) — not a consequence of the multi-project split itself, but it broke getting a clean pipeline run to verify the remaining items against. Full fix recorded below ("`schema_registry` ownership fix"). `verify-schedule` + the full `just test` suite were both re-run as part of verifying that fix and are now green; `DataModel.md`'s `schema_registry` section was also updated to document the ownership rule directly (`lakehouse_models.table_name`/`data_feed.batch_ods_name` DDL documentation gap closed the next day — see Phase 14's "`DataModel.md` corrections (2026-07-17)" entry).

## `schema_registry` ownership fix (2026-07-16)

Design settled in a dedicated implementation plan written in a prior session specifically so the fix wasn't designed in the same session that misdiagnosed it three times in a row (that plan file has been removed now that this work is done and fully captured here); implemented and verified in this one, with two real corrections made directly by the user during implementation (not guessed) — see `Learnings.md` for the durable lessons.

- **The rule, stated plainly**: schema discovery (and primary-key resolution) is exclusively the extraction step's job, complete before `clean` ever runs; `clean` only ever reads `schema_registry`, never writes it; `schema_registry` is never hand-seeded — a from-scratch feed or platform is expected to have zero rows there until that feed's first real run.
- **Every connector-driven feed's extraction step now owns discovery + `sync_schema_registry()`** (`connectors/schema_registry_sync.py`'s I/O side, `PostgresMetadataResource.sync_schema_registry()`), uniformly — this was already correct for tabular kinds (`postgres`/`csv`) but not for REST/JSON kinds, whose generated `clean_<feed>` bundled flatten+discover+sync together with validation, gated only on the `"validation"` pipeline step (meaning discovery silently never ran for a feed cherry-picked to extraction-only, and `clean` wasn't read-only the way the rule requires).
- **REST/JSON kinds' fix, not a straight port of the tabular shape**: flattening is what makes schema discovery possible for nested-JSON sources at all (`connectors/base.py`'s `JsonConnector`) — rather than flattening twice (once to discover, once to write), the extraction step now also performs the `clean`-layer write itself, reusing the one flattened DataFrame. `clean_<feed>` becomes a pure pass-through for these kinds, kept only so `clean.<feed>` has a stable `AssetKey` for dbt source lineage — there is no remaining validation-stage work for this connector shape. `raw_<feed>`'s own contract (verbatim, zero transformation) is completely unaffected — it still persists exactly the untouched nested fetch result it always did.
- **`customers`/`sales` (hand-written, predate the connector library) had the identical violation**, not just the codegen'd feeds — `clean_customers`/`clean_sales` also called `sync_schema_registry()` inline. Same fix applied (tabular shape, since these are flat synthetic generators, not nested).
- **`seed_schema_registry()` deleted outright**, along with its 4 call sites and the 4 dead hand-typed schema constants (`CUSTOMERS_SCHEMA`/`SALES_SCHEMA`/`FINANCIAL_TRANSACTIONS_SCHEMA`/`POLICE_CRIMES_SCHEMA`) — `metadata_runs` already had no seed row and bootstrapped correctly from a real run; every feed now follows that same pattern.
- **Terminology cleanup, by explicit direction**: the Dagster asset historically named `landing_<feed>` was renamed to `extraction_<feed>` everywhere (codegen templates + `extraction_assets.py`/`sales_assets.py` + `definitions.py`) — it never actually wrote to the `landing` storage layer, it's the extraction step's implementation (fetch from whatever the real source is); "landing" is only a meaningful concept when a feed's actual source *is* a landing directory (CSV/json_file kinds), not a generic pipeline-stage name. `data_processing_runs`' `landing_*` stage-tracking columns and the literal `data-lake/landing/` directory paths were deliberately left unrenamed — both are correctly named already, out of scope for this cleanup.
- **Second real bug, found during this fix's own live verification, not a regression from it and not in the original plan's scope**: `scripts/generate_deletion_synthesis_views.py` (generates `int_<feed>_with_deletes.sql`, the deletion-synthesis intermediate model) had its own unwarranted `schema_registry` dependency — it hard-crashed `just start` on a genuinely fresh platform, since `schema_registry` is correctly empty pre-first-run. Root-caused by reading the actual downstream consumer (`dbt/domains/sales/snapshots/sales_dim_customer.sql`, hand-written): it already names its own exact columns explicitly when selecting from this generated model, exactly the same way it already does selecting straight from staging — so the generated model never needed to enumerate columns from `schema_registry` at all. Fixed by switching its three `select <enumerated columns>` projections to `select *`, and deriving the join/anti-join predicate from `lakehouse_models.business_key_columns` (already-available metadata, no `schema_registry` involved) — removing the dependency entirely, not just tolerating a missing row. This also makes the "auto-generate all boilerplate the instant a user enters metadata" property strictly true for this model, where before it silently depended on a live run having already happened.
- **A doc/code drift found and fixed in passing**: this same script's output directory had drifted to `models/model/dimensions/generated/`; corrected to `models/model/intermediate/generated/`, matching README.md's own documented repo structure (`int_` is dbt's standard "intermediate model" naming convention — a reusable transformation step between staging and the final mart, neither raw staging nor itself the end-product dimension/fact). `.gitignore` updated to match; the stale pre-fix generated file removed.
- **Verified live**: full `just smoketest` (genuine nuke, rebuild) green — `verify-pipeline` 5/5 feed-run + 3/3 domain model-run, `verify-schedule` (real schedule-triggered run through the full webserver → daemon → `K8sRunLauncher` path) succeeded, 49/49 dbt tests, all pytest suites (`orchestration`, `frontend`, `tests/integration`). Directly confirmed via Postgres: every feed's current `schema_registry` row now has `created_by='extraction_<feed>'` (not a seed script), and `police_crimes` correctly resolved `primary_key_columns=["id"]` (previously silently empty due to the seed script never setting it) — the original ODS `COLUMN_NOT_FOUND` failure mode is gone, confirmed by regenerating ODS models twice in a row and getting byte-identical, keyed output both times.
- Hit the project's known `.pth`/`UF_HIDDEN` race once mid-verification (`dagster dev`'s long-running code server loaded `definitions.py` at exactly the wrong moment; short-lived `dagster job launch` subprocesses right next to it succeeded, since they got lucky on timing) — fixed per the standing rule (`rm -rf .venv && uv cache clean && uv sync --all-packages`, no diagnosis needed), then reran the full `smoketest` cycle clean.
